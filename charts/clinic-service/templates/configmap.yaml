{{- if .Values.config }}

apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ .Release.Name }}

  labels:
    app.kubernetes.io/name: {{ .Release.Name }}

data:

  {{- range $key, $value := .Values.config }}

  {{ $key }}: {{ $value | quote }}

  {{- end }}

{{- end }}