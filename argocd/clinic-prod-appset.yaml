apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet

metadata:
  name: clinic-prod
  namespace: argocd

spec:
  goTemplate: true

  goTemplateOptions:
    - missingkey=error

  generators:
    - git:
        repoURL: https://github.com/whospiko/clinic-gitops
        revision: main

        directories:
          - path: environments/prod/*

  template:

    metadata:
      name: 'prod-{{ .path.basename }}'

    spec:
      project: clinic

      source:
        repoURL: https://github.com/whospiko/clinic-gitops
        targetRevision: main

        path: charts/clinic-service

        helm:
          releaseName: '{{ .path.basename }}'

          valueFiles:
            - '../../{{ .path.path }}/values.yaml'

      destination:
        server: https://kubernetes.default.svc
        namespace: clinic-prod

      syncPolicy:
        automated:
          prune: true
          selfHeal: true

        syncOptions:
          - CreateNamespace=true